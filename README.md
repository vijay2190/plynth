# Plynth — Personal AI Dashboard

Monorepo:
- `web/` — React 19 + Vite + TS frontend (deployed to Netlify)
- `supabase/` — DB migrations + Edge Functions
- `mail-bridge/` — Local Flask relay reusing ngrok-remote-manager mailer

See `docs/` for setup + deploy notes.
